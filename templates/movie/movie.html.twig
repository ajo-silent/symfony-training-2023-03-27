{% extends 'base.html.twig' %}

{% block title %}Hello MovieController!{% endblock %}

{% block body %}
{#<style>#}
{#    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }#}
{#    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }#}
{#</style>#}

<div class="example-wrapper">
    {# ... if null ... Movie not found  #}

    <b>ID:</b> {{ movie.id }}<br/>
    <b>Title:</b> {{ movie.title }}<br/>
    <b>Released at:</b> {{ movie.releasedAt.format('Y-m-d') }}<br/>
    Oder so: <b>Released at:</b> {{ movie.releasedAt|date('Y-m-d') }}<br/>
    <b>Genres:</b>
    <ul>
        {% for genre in movie.genre %}
            <li>{{ genre }}</li>
        {% endfor %}
    </ul>
    Oder so: <b>Genres:</b> {{ movie.genre|join(', ') }}<br/>
{#    Oder so wenn es keine einfachen Strings sind: <b>Genres:</b> {{ movie.genre|map(genre => genre.name)|join(', ') }}<br/>#}
</div>
{% endblock %}
